export type FooterLinks = {
    title: string,
    text: string[]
}

export type Board = {
    id: string,
    name: string,
    title: string,
    group: string,
    image: string
}

export type awardsType = {
    id: number,
    img: string
}

export type newsType = {
    id: string,
    img: string,
    title: string,
    body: string,
    date: string,
    badges: string[]
}